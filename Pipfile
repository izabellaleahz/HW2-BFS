[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
networkx = "==2.5"
pytest = ">=3.0"

[dev-packages]

[requires]
python_version = "3.10"

[scripts]
test = "python3 -m pytest -v test/*"
